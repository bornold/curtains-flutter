name: curtains
description: This will open my curtains.

version: 1.0.0+1

environment:
  sdk: '>=2.1.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  ssh: ^0.0.3
  rxdart: ^0.22.0
  shared_preferences: ^0.5.2+2
  flutter_secure_storage: ^3.2.1+1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true